# agent of today.

- POST /register => user create /register karna
- POST /login => check username & password then loggedIn the user
- GET /user => get current logged in user profile details
- POST /logout => log out the user